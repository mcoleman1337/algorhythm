#!/usr/bin/python

class SynthEvent:
    
    def __init__:
        print "need to implement"
        #takes frequency, duration, a boolean specifying if duration absolute or tempo relative, an envelope, and optional synth specific parameters
        #envelop is a lambda created by an envelope generator, not yet implemented
